---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/app/layout.tsx
  - src/app/[locale]/layout.tsx
  - messages/en.json
  - messages/mm.json
  - src/components/LanguageSwitcher.tsx
autonomous: true
requirements:
  - FOUN-05
  - FOUN-06
  - UIUX-04

must_haves:
  truths:
    - "Visiting / redirects to /en (default locale)"
    - "Visiting /en renders English UI text; visiting /my renders Burmese UI text"
    - "The [locale]/layout.tsx applies font-myanmar class when locale is 'my', font-sans otherwise"
    - "Missing Burmese translation keys fall back to English (no untranslated key errors)"
    - "Language switcher appears in top-right header area on all pages"
    - "messages/en.json and messages/mm.json cover all Phase 1 UI strings (no hardcoded strings in JSX)"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "next-intl routing config with locales ['en', 'my'] and defaultLocale 'en'"
      contains: "localeDetection: false"
    - path: "src/i18n/request.ts"
      provides: "getRequestConfig that loads messages per locale"
    - path: "messages/en.json"
      provides: "English UI strings for all Phase 1 UI"
      contains: "login, nav, dashboard, errors keys"
    - path: "messages/mm.json"
      provides: "Burmese UI strings for all Phase 1 UI"
      contains: "login, nav, dashboard, errors keys"
    - path: "src/components/LanguageSwitcher.tsx"
      provides: "Top-right header language toggle between EN and MY"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "messages/[locale].json"
      via: "NextIntlClientProvider + getMessages()"
      pattern: "NextIntlClientProvider"
    - from: "src/app/[locale]/layout.tsx"
      to: "@fontsource-variable/noto-sans-myanmar"
      via: "font-myanmar CSS class when locale=my"
      pattern: "font-myanmar"
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware"
---

<objective>
Wire up next-intl URL-path bilingual routing (English at /en, Burmese at /my), create all Phase 1 translation files (en.json + mm.json), configure Noto Sans Myanmar Variable font loading conditional on locale, and add a language switcher component for the header.

Purpose: URL-path locale routing is locked and non-retroactive — all Phase 2-4 routes will inherit this structure. The translation files establish the i18n key naming convention all future features must follow.

Output: Visiting /en loads English UI, visiting /my loads Burmese UI with Noto Sans Myanmar font. All Phase 1 UI strings (login, nav, dashboard, errors) exist in both languages. Language switcher is visible and functional.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contracts from 01-01 and 01-02 (parallel wave) -->

next.config.ts (created in 01-01):
```typescript
import createNextIntlPlugin from 'next-intl/plugin';
const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');
export default withNextIntl({});
```
<!-- NOTE: next-intl plugin points to src/i18n/request.ts — this file MUST exist before build -->

Translation key naming convention (establish in this plan — all future plans inherit):
```
{section}.{key}
Examples:
  login.title, login.subtitle, login.username, login.password, login.submit, login.error
  nav.dashboard, nav.surveys, nav.settings, nav.signOut
  dashboard.welcome, dashboard.subtitle, dashboard.gettingStarted, dashboard.step1, dashboard.step2, dashboard.step3
  common.loading, common.error
  errors.invalidCredentials, errors.somethingWentWrong
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure next-intl routing, request config, and root/locale layouts</name>
  <files>
    src/i18n/routing.ts
    src/i18n/request.ts
    src/app/layout.tsx
    src/app/[locale]/layout.tsx
  </files>
  <read_first>
    - /Users/tindang/workspaces/tts/surey-yoma/.planning/phases/01-foundation/01-RESEARCH.md (lines 211-280: Pattern 1 URL-path i18n, code examples for routing.ts, request.ts, and locale layout)
    - /Users/tindang/workspaces/tts/surey-yoma/.planning/phases/01-foundation/01-CONTEXT.md (i18n Locale Strategy decisions)
    - src/middleware.ts (confirm createMiddleware(routing) import — created in 01-02, parallel wave)
    - next.config.ts (confirm ./src/i18n/request.ts path matches)
  </read_first>
  <action>
**Create `src/i18n/routing.ts`:**
```typescript
// src/i18n/routing.ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'my'],
  defaultLocale: 'en',
  localeDetection: false,  // URL path only — no Accept-Language browser detection
});
```

**Create `src/i18n/request.ts`:**
```typescript
// src/i18n/request.ts
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  // Await requestLocale (Next.js 15+ async params)
  let locale = await requestLocale;

  // Validate locale — fall back to default if invalid
  if (!locale || !routing.locales.includes(locale as 'en' | 'my')) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

**Update `src/app/layout.tsx`** — root layout (no html/body — locale layout handles that):
```tsx
// src/app/layout.tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  // Locale layout handles <html> and <body>
  return children;
}
```

**Update `src/app/[locale]/layout.tsx`** — locale layout with font, NextIntlClientProvider, and LanguageSwitcher:
```tsx
// src/app/[locale]/layout.tsx
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import '@fontsource-variable/noto-sans-myanmar';
import '../globals.css';
import { LanguageSwitcher } from '@/components/LanguageSwitcher';

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  const messages = await getMessages();

  return (
    <html lang={locale} suppressHydrationWarning>
      <body className={locale === 'my' ? 'font-myanmar' : 'font-sans'}>
        <NextIntlClientProvider messages={messages}>
          {/* Language switcher — top-right corner, visible on all pages */}
          <div className="fixed top-3 right-3 z-50">
            <LanguageSwitcher />
          </div>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

**Add Myanmar font CSS class to `src/app/globals.css`** — add to existing file, after existing content:

Open `src/app/globals.css` and add at the end:
```css
/* Noto Sans Myanmar Variable font — applied when locale is 'my' */
.font-myanmar {
  font-family: 'Noto Sans Myanmar Variable', 'Segoe UI', system-ui, sans-serif;
}
```
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/surey-yoma && npx next build 2>&1 | grep -E "error|Error|✓|✗" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "localeDetection: false" src/i18n/routing.ts` returns match
    - `grep "locales.*en.*my" src/i18n/routing.ts` returns match
    - `grep "defaultLocale.*en" src/i18n/routing.ts` returns match
    - `grep "NextIntlClientProvider" src/app/\[locale\]/layout.tsx` returns match
    - `grep "font-myanmar\|font-sans" src/app/\[locale\]/layout.tsx` returns match (conditional font class)
    - `grep "fontsource-variable/noto-sans-myanmar" src/app/\[locale\]/layout.tsx` returns match (font import)
    - `grep "font-myanmar" src/app/globals.css` returns match (CSS class defined)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>next-intl routing configured with locales ['en','my'], defaultLocale 'en', localeDetection false. Root layout is a passthrough. Locale layout wraps children in NextIntlClientProvider with correct messages, applies font-myanmar class for Burmese locale, imports Noto Sans Myanmar Variable font.</done>
</task>

<task type="auto">
  <name>Task 2: Create translation files (en.json + mm.json) and LanguageSwitcher component</name>
  <files>
    messages/en.json
    messages/mm.json
    src/components/LanguageSwitcher.tsx
  </files>
  <read_first>
    - /Users/tindang/workspaces/tts/surey-yoma/.planning/phases/01-foundation/01-CONTEXT.md (Admin Login UX, Admin Shell Layout decisions — derive all UI string keys from these)
    - /Users/tindang/workspaces/tts/surey-yoma/.planning/phases/01-foundation/01-RESEARCH.md (lines 492-510: Pitfall 6 on missing Burmese error strings)
    - src/app/[locale]/login/page.tsx (created in 01-02 — identify any hardcoded strings that need to move to messages)
    - src/components/admin/AdminSidebar.tsx (created in 01-02 — identify hardcoded nav label strings)
    - src/app/[locale]/(admin)/admin/page.tsx (created in 01-02 — identify hardcoded dashboard strings)
  </read_first>
  <action>
**Create `messages/en.json`** — ALL Phase 1 UI strings. Every string used in Phase 1 components must appear here. No hardcoded strings in JSX (FOUN-06 + UIUX-04).

```json
{
  "login": {
    "title": "Surey Yoma",
    "subtitle": "Admin Portal",
    "heading": "Sign in",
    "description": "Enter your credentials to access the admin panel",
    "username": "Username",
    "password": "Password",
    "submit": "Sign in",
    "submitting": "Signing in...",
    "error": "Invalid credentials",
    "networkError": "Something went wrong. Please try again."
  },
  "nav": {
    "brand": "Surey Yoma",
    "dashboard": "Dashboard",
    "surveys": "Surveys",
    "settings": "Settings",
    "signOut": "Sign out",
    "toggleExpand": "Expand sidebar",
    "toggleCollapse": "Collapse sidebar",
    "toggleMenu": "Toggle menu"
  },
  "dashboard": {
    "title": "Welcome to Surey Yoma",
    "subtitle": "Your employee engagement survey platform",
    "gettingStarted": "Getting Started",
    "gettingStartedDescription": "Complete these steps to run your first survey",
    "step1": "Configure SMTP email settings",
    "step2": "Create your first survey",
    "step3": "Import employee email list and send invitations"
  },
  "surveys": {
    "title": "Surveys",
    "comingSoon": "Survey management coming in Phase 2."
  },
  "settings": {
    "title": "Settings",
    "comingSoon": "SMTP configuration coming in Phase 2."
  },
  "common": {
    "loading": "Loading...",
    "back": "Back",
    "save": "Save",
    "cancel": "Cancel",
    "english": "English",
    "burmese": "မြန်မာ"
  },
  "errors": {
    "invalidCredentials": "Invalid credentials",
    "sessionExpired": "Your session has expired. Please sign in again.",
    "somethingWentWrong": "Something went wrong. Please try again.",
    "notFound": "Page not found",
    "unauthorized": "You are not authorized to view this page"
  }
}
```

**Create `messages/mm.json`** — Burmese translations for ALL keys above (UIUX-04 requires complete coverage):

```json
{
  "login": {
    "title": "Surey Yoma",
    "subtitle": "စီမံခန့်ခွဲသူ ပေါ်တယ်",
    "heading": "ဝင်ရောက်ရန်",
    "description": "စီမံခန့်ခွဲသူ ဧရိယာသို့ ဝင်ရောက်ရန် သင်၏ အထောက်အထားများကို ထည့်သွင်းပါ",
    "username": "အသုံးပြုသူ အမည်",
    "password": "စကားဝှက်",
    "submit": "ဝင်ရောက်ရန်",
    "submitting": "ဝင်ရောက်နေသည်...",
    "error": "အထောက်အထားများ မမှန်ကန်ပါ",
    "networkError": "တစ်ခုခု မှားယွင်းသွားသည်။ ထပ်မံကြိုးစားပါ။"
  },
  "nav": {
    "brand": "Surey Yoma",
    "dashboard": "ဒက်ရှ်ဘုတ်",
    "surveys": "စစ်တမ်းများ",
    "settings": "ဆက်တင်များ",
    "signOut": "ထွက်ရန်",
    "toggleExpand": "ဘေးဘားများ ချဲ့ရန်",
    "toggleCollapse": "ဘေးဘားများ ချုံ့ရန်",
    "toggleMenu": "မီနူး ဖွင့်/ပိတ်ရန်"
  },
  "dashboard": {
    "title": "Surey Yoma သို့ ကြိုဆိုပါသည်",
    "subtitle": "သင်၏ ဝန်ထမ်းပါဝင်မှု စစ်တမ်း ပလပ်ဖောင်း",
    "gettingStarted": "စတင်ရန်",
    "gettingStartedDescription": "သင်၏ ပထမဆုံး စစ်တမ်းကို လုပ်ဆောင်ရန် ဤအဆင့်များကို ပြီးမြောက်ပါ",
    "step1": "SMTP အီးမေးလ် ဆက်တင်များကို ပြင်ဆင်ပါ",
    "step2": "သင်၏ ပထမဆုံး စစ်တမ်းကို ဖန်တီးပါ",
    "step3": "ဝန်ထမ်း အီးမေးလ် စာရင်း တင်သွင်းပြီး ဖိတ်ကြားချက်များ ပို့ပါ"
  },
  "surveys": {
    "title": "စစ်တမ်းများ",
    "comingSoon": "စစ်တမ်း စီမံခန့်ခွဲမှုကို အဆင့် ၂ တွင် ဆောင်ရွက်မည်။"
  },
  "settings": {
    "title": "ဆက်တင်များ",
    "comingSoon": "SMTP ပြင်ဆင်ချက်ကို အဆင့် ၂ တွင် ဆောင်ရွက်မည်။"
  },
  "common": {
    "loading": "ဖွင့်နေသည်...",
    "back": "နောက်သို့",
    "save": "သိမ်းဆည်းရန်",
    "cancel": "မလုပ်တော့ပါ",
    "english": "English",
    "burmese": "မြန်မာ"
  },
  "errors": {
    "invalidCredentials": "အထောက်အထားများ မမှန်ကန်ပါ",
    "sessionExpired": "သင်၏ ဆက်ရှင် သက်တမ်းကုန်သွားပြီ။ ထပ်မံ ဝင်ရောက်ပါ။",
    "somethingWentWrong": "တစ်ခုခု မှားယွင်းသွားသည်။ ထပ်မံကြိုးစားပါ။",
    "notFound": "စာမျက်နှာ မတွေ့ပါ",
    "unauthorized": "ဤစာမျက်နှာကို ကြည့်ရှုခွင့် မရှိပါ"
  }
}
```

**Create `src/components/LanguageSwitcher.tsx`** — locale toggle button in top-right header, visible on all pages:

```tsx
// src/components/LanguageSwitcher.tsx
'use client';

import { useLocale, useTranslations } from 'next-intl';
import { useRouter, usePathname } from 'next/navigation';
import { Button } from '@/components/ui/button';

export function LanguageSwitcher() {
  const locale = useLocale();
  const pathname = usePathname();
  const router = useRouter();
  const t = useTranslations('common');

  function switchLocale() {
    const targetLocale = locale === 'en' ? 'my' : 'en';
    // Replace current locale prefix in pathname
    // e.g. /en/admin -> /my/admin, /my/login -> /en/login
    const newPath = pathname.replace(/^\/(en|my)/, `/${targetLocale}`);
    router.push(newPath);
  }

  return (
    <Button
      variant="outline"
      size="sm"
      onClick={switchLocale}
      className="text-xs font-medium bg-white/90 backdrop-blur-sm border-gray-200 hover:bg-gray-50"
      aria-label={locale === 'en' ? 'Switch to Burmese' : 'Switch to English'}
    >
      {locale === 'en' ? t('burmese') : t('english')}
    </Button>
  );
}
```

**Update components created in 01-02 to use translations** (UIUX-04 — no hardcoded UI strings):

In `src/app/[locale]/login/page.tsx`, replace hardcoded strings with `useTranslations('login')` calls:
- `'Sign in'` (heading) → `t('heading')`
- `'Username'` → `t('username')`
- `'Password'` → `t('password')`
- `'Sign in'` (button) → `t('submit')`
- `'Signing in...'` → `t('submitting')`
- `'Invalid credentials'` (error) → `t('error')`

In `src/components/admin/AdminSidebar.tsx`, replace hardcoded strings with `useTranslations('nav')` calls:
- `'Surey Yoma'` (brand) → `t('brand')`
- `'Dashboard'`, `'Surveys'`, `'Settings'` → `t('dashboard')`, `t('surveys')`, `t('settings')`
- `'Sign out'` → `t('signOut')`

In `src/app/[locale]/(admin)/admin/page.tsx`, replace hardcoded strings with `useTranslations('dashboard')` calls.

**Note:** These updates must be done carefully — add `'use client'` to components that call `useTranslations` if they aren't already client components. Server components can use `useTranslations` from `next-intl/server` (or the server-side `getTranslations`). For server components (RSC), use:
```typescript
import { getTranslations } from 'next-intl/server';
const t = await getTranslations('dashboard');
```
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/surey-yoma && npx vitest run --reporter=verbose __tests__/lib/csv.service.test.ts __tests__/lib/constants.test.ts __tests__/lib/storage.test.ts __tests__/lib/auth.test.ts && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cat messages/en.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(len([k for s in d.values() for k in s]))"` returns >= 30 (all string keys present)
    - `grep '"login"' messages/en.json messages/mm.json` returns match in both files
    - `grep '"nav"' messages/en.json messages/mm.json` returns match in both files
    - `grep '"dashboard"' messages/en.json messages/mm.json` returns match in both files
    - `grep '"errors"' messages/en.json messages/mm.json` returns match in both files
    - `grep "useTranslations\|getTranslations" src/app/\[locale\]/login/page.tsx` returns match
    - `grep "LanguageSwitcher" src/app/\[locale\]/layout.tsx` returns match (switcher mounted in layout)
    - `grep "useLocale" src/components/LanguageSwitcher.tsx` returns match
    - `npx next build` exits 0
    - `grep -r "Sign in\|Dashboard\|Surveys\|Settings\|Sign out" src/app src/components --include="*.tsx" -l | wc -l` returns 0 (no hardcoded English UI strings in JSX — they should all be in messages)
  </acceptance_criteria>
  <done>messages/en.json and messages/mm.json both contain login, nav, dashboard, surveys, settings, common, and errors sections. LanguageSwitcher mounts in locale layout header. All Phase 1 components use useTranslations / getTranslations instead of hardcoded strings. Build exits 0.</done>
</task>

</tasks>

<verification>
Run full test suite and build:
```bash
cd /Users/tindang/workspaces/tts/surey-yoma && npx vitest run && npx next build
```

Verify i18n routing manually:
1. `npm run dev`
2. Visit http://localhost:3000 — confirm redirect to /en
3. Visit http://localhost:3000/en — confirm English UI
4. Click language switcher — confirm redirect to /my with Burmese UI
5. Verify Noto Sans Myanmar font applied on /my (check body class in DevTools: `font-myanmar`)
6. Visit http://localhost:3000/my/admin — confirm Burmese admin dashboard
7. Click language switcher — confirm back to /en/admin
</verification>

<success_criteria>
- `grep "localeDetection: false" src/i18n/routing.ts` returns match
- `grep "locales.*en.*my" src/i18n/routing.ts` returns match
- Both `messages/en.json` and `messages/mm.json` contain keys: login.title, nav.dashboard, dashboard.title, errors.invalidCredentials
- `grep "font-myanmar" src/app/\[locale\]/layout.tsx` returns match (conditional font class)
- `grep "LanguageSwitcher" src/app/\[locale\]/layout.tsx` returns match
- `npx next build` exits 0
- `npx vitest run` exits 0 (all tests green)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` with:
- Files created/modified
- Translation key naming convention established (for future phases to follow)
- Any Burmese translation gaps noted
- Build and test results
</output>
