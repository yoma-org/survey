---
phase: 02-survey-creation-and-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/services/csv.service.ts
  - src/lib/services/survey.service.ts
  - src/app/api/surveys/route.ts
  - src/app/api/surveys/[id]/route.ts
  - src/app/api/surveys/[id]/questions/route.ts
  - src/app/[locale]/(admin)/admin/surveys/page.tsx
  - src/app/[locale]/(admin)/admin/surveys/new/page.tsx
  - src/app/[locale]/(admin)/admin/surveys/[id]/page.tsx
  - src/components/admin/SurveyCreateForm.tsx
  - src/components/admin/ExcelUploadStep.tsx
  - messages/en.json
  - messages/mm.json
  - __tests__/services/survey.service.test.ts
  - __tests__/services/excel.service.test.ts
autonomous: true
requirements:
  - SURV-01
  - SURV-02
  - SURV-03
  - SURV-04
  - SURV-05
  - SURV-06
  - DATA-01
  - DATA-03

must_haves:
  truths:
    - "Admin sees a list of surveys with name, status badge, question count, and created date"
    - "Admin can create a survey by filling name + description and then uploading an .xlsx file"
    - "Uploading a valid Excel file shows a question preview table with English and Burmese columns"
    - "Questions are stored in questions-{surveyId}.csv and survey metadata in surveys.csv"
    - "Survey questions include Likert, open-ended, and demographic types with dimension assignment"
  artifacts:
    - path: "src/lib/services/survey.service.ts"
      provides: "Survey CRUD: create, list, get by ID — reads/writes surveys.csv and questions-{id}.csv"
      exports: ["createSurvey", "listSurveys", "getSurvey", "saveQuestions", "getQuestions"]
    - path: "src/app/api/surveys/route.ts"
      provides: "GET /api/surveys (list), POST /api/surveys (create)"
    - path: "src/app/api/surveys/[id]/questions/route.ts"
      provides: "POST /api/surveys/[id]/questions — receives FormData with .xlsx, returns parsed questions JSON"
    - path: "src/app/[locale]/(admin)/admin/surveys/page.tsx"
      provides: "Survey list page with real data from surveys.csv"
    - path: "src/app/[locale]/(admin)/admin/surveys/new/page.tsx"
      provides: "Two-step survey creation: name/description then Excel upload"
    - path: "__tests__/services/survey.service.test.ts"
      provides: "Wave 0 test stubs for survey CRUD"
    - path: "__tests__/services/excel.service.test.ts"
      provides: "Wave 0 test stubs for Excel parsing"
  key_links:
    - from: "src/components/admin/ExcelUploadStep.tsx"
      to: "/api/surveys/[id]/questions"
      via: "FormData POST with file field"
      pattern: "formData.*file.*POST.*questions"
    - from: "src/app/api/surveys/[id]/questions/route.ts"
      to: "exceljs workbook.xlsx.load(buffer)"
      via: "file.arrayBuffer() -> Buffer"
      pattern: "workbook\\.xlsx\\.load"
    - from: "src/lib/services/survey.service.ts"
      to: "src/lib/services/csv.service.ts"
      via: "readRows, appendRow, writeRows"
      pattern: "readRows|appendRow|writeRows"
---

<objective>
Build the survey CRUD layer, Excel import pipeline, and admin survey pages.

Purpose: Provides the foundation for the survey lifecycle — admins can create surveys, import bilingual questions from Excel, and view all surveys. Plan 02 depends on survey IDs existing before tokens and email dispatch can work.

Output: survey.service.ts, Excel import API route, survey list/create/detail pages, and question storage in per-survey CSV files.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-survey-creation-and-distribution/02-CONTEXT.md
@.planning/phases/02-survey-creation-and-distribution/02-UI-SPEC.md
@.planning/phases/02-survey-creation-and-distribution/02-VALIDATION.md
@src/lib/types.ts
@src/lib/services/csv.service.ts
@src/lib/storage/index.ts
@messages/en.json

<interfaces>
<!-- Executor: use these directly — no exploration needed -->

From src/lib/types.ts (existing):
```typescript
export type QuestionType = 'likert' | 'open_ended' | 'demographic';
export type Dimension = 'camaraderie' | 'credibility' | 'fairness' | 'pride' | 'respect' | 'uncategorized';

export interface Question {
  id: string;           // e.g. "CAM-01", "OE-01", "DEM-ORG"
  type: QuestionType;
  dimension?: Dimension;
  subPillar?: string;
  en: string;
  my: string;
  options?: { en: string; my: string }[];
}

export interface Survey {
  id: string;
  name: string;
  description?: string;
  status: 'draft' | 'active' | 'closed';
  createdAt: string;   // ISO string
}
```

From src/lib/services/csv.service.ts (existing — note: writeRows does NOT exist yet, add it):
```typescript
export function parseCSV<T>(content: string): T[]
export function serializeCSV<T>(rows: T[]): string
export async function readRows<T>(filename: string): Promise<T[]>
export async function appendRow(filename: string, row: Record<string, string>): Promise<void>
// writeRows() is MISSING — Task 1 must add it (full overwrite, no ETag retry needed)
```

From src/lib/storage/index.ts:
```typescript
export function getStorageAdapter(): StorageAdapter
// adapter.read(filename) -> string | null
// adapter.write(filename, content) -> Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, extend csv.service.ts with writeRows, add SmtpSettings type, and create Wave 0 test stubs</name>
  <read_first>
    - src/lib/services/csv.service.ts (MUST read — adding writeRows here)
    - src/lib/types.ts (MUST read — adding SmtpSettings interface)
    - .planning/phases/02-survey-creation-and-distribution/02-VALIDATION.md (Wave 0 test stubs listed)
  </read_first>
  <files>
    src/lib/services/csv.service.ts,
    src/lib/types.ts,
    __tests__/services/survey.service.test.ts,
    __tests__/services/excel.service.test.ts,
    __tests__/services/token.service.test.ts,
    __tests__/services/email.service.test.ts,
    __tests__/services/smtp.service.test.ts
  </files>
  <behavior>
    - writeRows(filename, rows) writes the full CSV from scratch (no ETag retry — used for single-row overwrite like smtp-settings.csv)
    - SmtpSettings interface has: host, port, username, password, fromAddress, fromName (all string)
    - Survey service test stub: describe('survey.service') with it.todo for createSurvey, listSurveys, saveQuestions, getQuestions
    - Excel service test stub: describe('excel.service') with it.todo for parseExcelBuffer returns Question[], validateRow rejects missing questionId
    - Token service test stub: describe('token.service') with it.todo for generateToken returns 64-char hex, generateToken is idempotent for same email+surveyId
    - Email service test stub: describe('email.service') with it.todo for sendInvitation calls transporter.sendMail
    - SMTP service test stub: describe('smtp.service') with it.todo for saveSmtpSettings, getSmtpSettings
  </behavior>
  <action>
    1. Install new dependencies:
       ```
       npm install nodemailer exceljs sonner
       npm install -D @types/nodemailer
       npx shadcn@latest add dialog badge select textarea alert progress
       ```

    2. Add `writeRows` to src/lib/services/csv.service.ts AFTER the existing `appendRow` function:
       ```typescript
       /**
        * Write (overwrite) all rows to a named CSV file.
        * Use for single-row files like smtp-settings.csv where full overwrite is correct.
        * Does NOT use ETag retry — caller is responsible for avoiding concurrent writes.
        */
       export async function writeRows(
         filename: string,
         rows: Record<string, string>[]
       ): Promise<void> {
         const adapter = getStorageAdapter();
         const csv = serializeCSV(rows);
         await adapter.write(filename, csv);
       }
       ```

    3. Add `SmtpSettings` interface to src/lib/types.ts:
       ```typescript
       export interface SmtpSettings {
         host: string;
         port: string;       // stored as string in CSV
         username: string;
         password: string;   // stored as-is; never transmitted to client
         fromAddress: string;
         fromName: string;
       }
       ```

    4. Create `__tests__/` directory tree and all 5 stub test files. Each file must:
       - Import vitest (`describe`, `it`, `expect`)
       - Have a `describe` block matching the service name
       - Have `it.todo('...')` stubs for each behavior listed above
       - NOT import the actual service files yet (stubs only — services don't exist yet)
       - Pass `npx vitest run` without errors (todos are passing stubs)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "writeRows" src/lib/services/csv.service.ts` returns the new function
    - `grep -n "SmtpSettings" src/lib/types.ts` returns the interface
    - `npx vitest run` exits 0 with all test stubs shown as "todo"
    - `ls __tests__/services/` shows all 5 stub files
  </acceptance_criteria>
  <done>writeRows added to csv.service.ts, SmtpSettings in types.ts, all 5 Wave 0 test stubs exist and vitest passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: survey.service.ts — Survey CRUD and question storage with CSV persistence</name>
  <read_first>
    - src/lib/types.ts (Survey, Question interfaces)
    - src/lib/services/csv.service.ts (readRows, appendRow, writeRows — Task 1 must be done)
    - __tests__/services/survey.service.test.ts (stubs to fill in)
    - .planning/phases/02-survey-creation-and-distribution/02-CONTEXT.md (data model decisions)
  </read_first>
  <files>
    src/lib/services/survey.service.ts,
    src/app/api/surveys/route.ts,
    src/app/api/surveys/[id]/route.ts,
    src/app/api/surveys/[id]/questions/route.ts,
    __tests__/services/survey.service.test.ts
  </files>
  <behavior>
    - createSurvey({ name, description }): generates nanoid-style ID (use crypto.randomUUID()), sets status='draft', createdAt=ISO, appends to surveys.csv
    - listSurveys(): reads surveys.csv, returns Survey[] sorted by createdAt desc
    - getSurvey(id): reads surveys.csv, returns Survey | null
    - saveQuestions(surveyId, questions): writes to questions-{surveyId}.csv via writeRows (full overwrite — questions are replaced on re-import)
    - getQuestions(surveyId): reads questions-{surveyId}.csv, returns Question[]
    - Question rows in CSV: id, type, dimension, subPillar, en, my, optionsJson (JSON string or empty)
    - POST /api/surveys returns 201 + { survey } on success; 400 if name is missing
    - GET /api/surveys returns 200 + { surveys: Survey[] }
    - GET /api/surveys/[id] returns 200 + { survey, questions } or 404
    - POST /api/surveys/[id]/questions: receives FormData with 'file' field (.xlsx), parses with exceljs, saves to questions-{id}.csv, returns 200 + { questions, count }
    - exceljs: row.values is 1-indexed — always slice(1) to get columns as 0-indexed array
    - Expected Excel columns (0-indexed after slice): [0]=questionId, [1]=type, [2]=englishText, [3]=burmeseText, [4]=dimension, [5]=subPillar, [6]=optionsJson
    - Skip header row (rowNumber === 1)
    - Validate questionId is non-empty; skip rows where questionId is blank
    - All API routes must check iron-session auth: import { getIronSession } from 'iron-session'; import { sessionOptions } from '@/lib/auth'
  </behavior>
  <action>
    Create `src/lib/services/survey.service.ts`:
    ```typescript
    import { randomUUID } from 'crypto';
    import { readRows, appendRow, writeRows } from './csv.service';
    import type { Survey, Question } from '@/lib/types';

    const SURVEYS_FILE = 'surveys.csv';

    export async function createSurvey(data: { name: string; description?: string }): Promise<Survey> {
      const survey: Survey = {
        id: randomUUID(),
        name: data.name,
        description: data.description ?? '',
        status: 'draft',
        createdAt: new Date().toISOString(),
      };
      await appendRow(SURVEYS_FILE, survey as unknown as Record<string, string>);
      return survey;
    }

    export async function listSurveys(): Promise<Survey[]> {
      const rows = await readRows<Survey>(SURVEYS_FILE);
      return rows.sort((a, b) => b.createdAt.localeCompare(a.createdAt));
    }

    export async function getSurvey(id: string): Promise<Survey | null> {
      const rows = await readRows<Survey>(SURVEYS_FILE);
      return rows.find(s => s.id === id) ?? null;
    }

    export async function saveQuestions(surveyId: string, questions: Question[]): Promise<void> {
      const rows = questions.map(q => ({
        id: q.id,
        type: q.type,
        dimension: q.dimension ?? '',
        subPillar: q.subPillar ?? '',
        en: q.en,
        my: q.my,
        optionsJson: q.options ? JSON.stringify(q.options) : '',
      }));
      await writeRows(`questions-${surveyId}.csv`, rows);
    }

    export async function getQuestions(surveyId: string): Promise<Question[]> {
      const rows = await readRows<Record<string, string>>(`questions-${surveyId}.csv`);
      return rows.map(r => ({
        id: r.id,
        type: r.type as Question['type'],
        dimension: (r.dimension || undefined) as Question['dimension'],
        subPillar: r.subPillar || undefined,
        en: r.en,
        my: r.my,
        options: r.optionsJson ? JSON.parse(r.optionsJson) : undefined,
      }));
    }
    ```

    Create API routes following the established iron-session auth pattern from /api route files in Phase 1. Each route must:
    - Import `getIronSession` from 'iron-session' and `sessionOptions` from '@/lib/auth'
    - Check session.isLoggedIn — return 401 if not authenticated
    - Call the appropriate survey.service function
    - Return Response.json({...})

    For POST /api/surveys/[id]/questions:
    - `const formData = await request.formData()`
    - `const file = formData.get('file') as File`
    - `const buffer = Buffer.from(await file.arrayBuffer())`
    - `const workbook = new ExcelJS.Workbook()`
    - `await workbook.xlsx.load(buffer)`
    - `const sheet = workbook.worksheets[0]`
    - Loop with `sheet.eachRow((row, rowNumber) => { if (rowNumber === 1) return; const vals = row.values.slice(1) as string[]; ... })`

    Update `__tests__/services/survey.service.test.ts` — replace todos with real tests:
    - Mock csv.service.ts using `vi.mock('@/lib/services/csv.service')`
    - Test createSurvey: verify appendRow called with SURVEYS_FILE and row containing name
    - Test listSurveys: verify returns sorted array when readRows returns data
    - Test getSurvey: returns null when not found
    - Test saveQuestions: verify writeRows called with correct filename pattern `questions-${surveyId}.csv`
  </action>
  <verify>
    <automated>npx vitest run survey --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function createSurvey" src/lib/services/survey.service.ts` returns match
    - `grep -n "export async function saveQuestions" src/lib/services/survey.service.ts` returns match
    - `grep -rn "workbook.xlsx.load" src/app/api/surveys/` returns match
    - `npx vitest run survey` exits 0 with all survey service tests passing
    - `ls src/app/api/surveys/` shows route.ts and [id]/ subdirectory
    - `ls src/app/api/surveys/\[id\]/questions/` shows route.ts
  </acceptance_criteria>
  <done>survey.service.ts created with 5 exported functions, all 3 API route files created with auth guards, vitest survey tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Survey pages — list, create form, detail/questions view, and translation strings</name>
  <read_first>
    - src/app/[locale]/(admin)/admin/surveys/page.tsx (existing stub — will be replaced)
    - src/app/[locale]/(admin)/layout.tsx (admin layout pattern)
    - src/components/admin/AdminSidebar.tsx (sidebar structure to understand layout)
    - .planning/phases/02-survey-creation-and-distribution/02-UI-SPEC.md (component inventory, copywriting contract)
    - messages/en.json (existing keys — extend, do not replace)
  </read_first>
  <files>
    src/app/[locale]/(admin)/admin/surveys/page.tsx,
    src/app/[locale]/(admin)/admin/surveys/new/page.tsx,
    src/app/[locale]/(admin)/admin/surveys/[id]/page.tsx,
    src/components/admin/SurveyCreateForm.tsx,
    src/components/admin/ExcelUploadStep.tsx,
    messages/en.json,
    messages/mm.json
  </files>
  <action>
    **Install remaining shadcn components if not done in Task 1:**
    Ensure Badge, Alert, Progress are available in src/components/ui/.

    **Translation strings** — add to BOTH messages/en.json and messages/mm.json under "surveys" key.
    Replace the existing `surveys.comingSoon` stub. Add all keys the components reference.

    English additions to messages/en.json `surveys` section:
    ```json
    "surveys": {
      "title": "Surveys",
      "newSurvey": "New Survey",
      "createFirstSurvey": "Create Your First Survey",
      "emptyHeading": "No surveys yet",
      "emptyBody": "Create a survey to start collecting employee feedback.",
      "createTitle": "Create Survey",
      "nameLabel": "Survey Name",
      "namePlaceholder": "e.g. Annual Employee Engagement 2026",
      "nameRequired": "Survey name is required.",
      "descriptionLabel": "Description",
      "descriptionPlaceholder": "Optional description for this survey",
      "createButton": "Create Survey",
      "discardButton": "Discard Changes",
      "uploadLabel": "Import Questions",
      "uploadHint": "Upload an Excel file to preview questions before saving.",
      "uploadDropzone": "Drag and drop your .xlsx file here, or click to browse",
      "uploadFormatError": "File must be an .xlsx or .xls file.",
      "uploadParseError": "Could not read questions from this file. Check that columns match the expected format.",
      "uploadSuccess": "{count} questions imported",
      "importButton": "Import Questions",
      "questionId": "ID",
      "questionEnglish": "English",
      "questionBurmese": "Burmese",
      "questionType": "Type",
      "questionSection": "Section",
      "statusDraft": "Draft",
      "statusActive": "Active",
      "statusClosed": "Closed",
      "questionCount": "{count} questions",
      "viewButton": "View",
      "deleteButton": "Delete",
      "deleteConfirmHeading": "Delete this survey?",
      "deleteConfirmBody": "All associated questions and invitation links will be removed permanently.",
      "deleteConfirmAction": "Delete Survey",
      "deleteKeepAction": "Keep Survey",
      "typeLikert": "Likert",
      "typeOpenEnded": "Open-ended",
      "typeDemographic": "Demographic"
    }
    ```

    Burmese additions to messages/mm.json mirror the same keys. Use these exact Burmese values:
    ```json
    "surveys": {
      "title": "စစ်တမ်းများ",
      "newSurvey": "စစ်တမ်းအသစ်",
      "createFirstSurvey": "ပထမဆုံးစစ်တမ်းဖန်တီးရန်",
      "emptyHeading": "စစ်တမ်းမရှိသေးပါ",
      "emptyBody": "ဝန်ထမ်းများ၏ အကြံပြုချက်စောင့်ဆိုင်းရန် စစ်တမ်းတစ်ခုဖန်တီးပါ။",
      "createTitle": "စစ်တမ်းဖန်တီးရန်",
      "nameLabel": "စစ်တမ်းအမည်",
      "namePlaceholder": "ဥပမာ - ဝန်ထမ်းပူးပေါင်းဆောင်ရွက်မှု ၂၀၂၆",
      "nameRequired": "စစ်တမ်းအမည် ဖြည့်ရန် လိုအပ်သည်။",
      "descriptionLabel": "ဖော်ပြချက်",
      "descriptionPlaceholder": "ဤစစ်တမ်းအတွက် ဖော်ပြချက် (မဖြစ်မနေမဟုတ်)",
      "createButton": "စစ်တမ်းဖန်တီးရန်",
      "discardButton": "ပယ်ဖျက်ရန်",
      "uploadLabel": "မေးခွန်းများတင်သွင်းရန်",
      "uploadHint": "မေးခွန်းများကို ကြည့်ရှုမည်ဆိုပါက Excel ဖိုင်တင်သွင်းပါ။",
      "uploadDropzone": ".xlsx ဖိုင်ကို ဤနေရာတွင် ဆွဲချပါ သို့မဟုတ် ကလစ်နှိပ်ပါ",
      "uploadFormatError": "ဖိုင်သည် .xlsx သို့မဟုတ် .xls ဖိုင်ဖြစ်ရမည်။",
      "uploadParseError": "ဤဖိုင်မှ မေးခွန်းများဖတ်၍မရပါ။ ကော်လံဖော်မတ်ကို စစ်ဆေးပါ။",
      "uploadSuccess": "မေးခွန်း {count} ခု တင်သွင်းပြီး",
      "importButton": "မေးခွန်းများတင်သွင်းရန်",
      "questionId": "ID",
      "questionEnglish": "အင်္ဂလိပ်",
      "questionBurmese": "မြန်မာ",
      "questionType": "အမျိုးအစား",
      "questionSection": "အပိုင်း",
      "statusDraft": "မူကြမ်း",
      "statusActive": "တက်ကြွ",
      "statusClosed": "ပိတ်ပြီး",
      "questionCount": "မေးခွန်း {count} ခု",
      "viewButton": "ကြည့်ရှုရန်",
      "deleteButton": "ဖျက်ရန်",
      "deleteConfirmHeading": "ဤစစ်တမ်းကို ဖျက်မည်လား?",
      "deleteConfirmBody": "ဆက်စပ်မေးခွန်းများနှင့် ဖိတ်ကြားချက်လင့်ခ်များ အပြီးအပိုင်ဖယ်ရှားမည်။",
      "deleteConfirmAction": "စစ်တမ်းဖျက်ရန်",
      "deleteKeepAction": "ထိန်းသိမ်းရန်",
      "typeLikert": "Likert",
      "typeOpenEnded": "ပွင့်လင်းမေးခွန်း",
      "typeDemographic": "လူဦးရေ"
    }
    ```

    **src/app/[locale]/(admin)/admin/surveys/page.tsx** — Replace stub with real server component:
    - `import { listSurveys } from '@/lib/services/survey.service'`
    - `import { getTranslations } from 'next-intl/server'`
    - Render list of surveys using shadcn Card per row (or table layout)
    - Each row: survey name (bold), status Badge, question count, createdAt date, "View" Button (outline), inline delete confirmation
    - Status badge variants: Draft = `<Badge variant="secondary">`, Active = `<Badge className="bg-blue-50 text-blue-700 border-0">`, Closed = `<Badge variant="outline">`
    - "New Survey" Button (default/blue) top-right linking to `/admin/surveys/new`
    - Empty state: heading + body + "Create Your First Survey" Button per UI spec
    - Page title: 20px semibold, "Surveys"

    **src/app/[locale]/(admin)/admin/surveys/new/page.tsx** — Two-step creation page:
    - Step 1 rendered by `<SurveyCreateForm>` client component
    - After step 1 POST succeeds (survey created), render `<ExcelUploadStep surveyId={id}>` client component
    - Use `'use client'` on page OR keep as server component and pass initial state via URL search param `?step=2&id={surveyId}`

    **src/components/admin/SurveyCreateForm.tsx** — `'use client'` component:
    - Form with shadcn Input (Survey Name, required) + Textarea (Description, optional)
    - Inline validation: if name is empty on submit, show error text "Survey name is required." below field; set `aria-invalid="true"` on input
    - On submit: POST to `/api/surveys` with `{ name, description }`
    - On success: call `onSuccess(surveyId)` prop OR use `router.push('/admin/surveys/new?step=2&id=' + surveyId)`
    - Buttons: "Create Survey" (default, blue) + "Discard Changes" (outline, links to /admin/surveys)
    - All labels via `useTranslations('surveys')`

    **src/components/admin/ExcelUploadStep.tsx** — `'use client'` component:
    - Props: `surveyId: string`
    - Drop zone: `<div>` with `onDragOver`, `onDrop`, `onClick` handlers; CSS: `border-2 border-dashed rounded-lg min-h-[120px] flex items-center justify-center cursor-pointer`; on dragOver: `border-blue-500`
    - Hidden `<input type="file" accept=".xlsx,.xls">` triggered by drop zone click
    - Accept .xlsx and .xls only; on invalid type: show error "File must be an .xlsx or .xls file."
    - Upload feedback row (after file selected): `<FileSpreadsheet>` icon (lucide), filename, file size, `<button aria-label="Remove file"><X /></button>`
    - On upload: POST FormData with file to `/api/surveys/${surveyId}/questions`
    - Show indeterminate spinner while parsing
    - On success: show shadcn Alert (success variant) "N questions imported", render question preview table
    - On failure: show Alert (destructive variant) with error message
    - Question preview table columns: ID | English | Burmese | Type | Section
    - Type badges: Likert = `<Badge>` blue, Open-ended = `<Badge variant="secondary">`, Demographic = `<Badge variant="outline">`
    - After successful import show "Import Questions" Button that navigates to `/admin/surveys/${surveyId}`

    **src/app/[locale]/(admin)/admin/surveys/[id]/page.tsx** — Survey detail server component:
    - Calls `getSurvey(id)` and `getQuestions(id)` from survey.service.ts
    - Returns 404 (notFound()) if survey not found
    - Shows survey name, status badge, description, created date
    - Shows question count
    - Renders question table (same columns as preview table in ExcelUploadStep)
    - "Send Invitations" Button (default, blue) linking to future `/admin/surveys/${id}/invite`
    - Back link to /admin/surveys
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "listSurveys\|getSurvey" src/app/\[locale\]` returns at least 2 matches
    - `grep -n "ExcelUploadStep" src/components/admin/ExcelUploadStep.tsx` returns match
    - `grep -n "workbook\|exceljs\|ExcelJS" src/app/api/surveys/` returns match (exceljs used in questions route)
    - `grep -n "statusDraft\|statusActive\|statusClosed" messages/en.json` returns all 3 matches
    - `grep -n "statusDraft\|statusActive\|statusClosed" messages/mm.json` returns all 3 matches
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Survey list shows real data from CSV, create form works as two-step flow, question preview table renders after Excel upload, translations complete in both languages</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the full plan:

1. `npx vitest run --reporter=verbose` — all tests green (no failures, todos are ok)
2. `npx tsc --noEmit` — zero TypeScript errors
3. `grep -n "writeRows" src/lib/services/csv.service.ts` — function exists
4. `grep -n "SmtpSettings" src/lib/types.ts` — interface defined
5. `ls src/lib/services/survey.service.ts` — file exists
6. `ls src/app/api/surveys/route.ts src/app/api/surveys/\[id\]/route.ts src/app/api/surveys/\[id\]/questions/route.ts` — all 3 API routes exist
7. `ls src/app/\[locale\]/\(admin\)/admin/surveys/new/page.tsx src/app/\[locale\]/\(admin\)/admin/surveys/\[id\]/page.tsx` — both pages exist
8. `grep -c "surveys\." messages/en.json` — count reflects new keys added
9. `grep -c "surveys\." messages/mm.json` — same count as en.json
</verification>

<success_criteria>
- surveys.csv stores survey metadata; questions-{id}.csv stores per-survey questions (DATA-03)
- Admin can create a survey via POST /api/surveys and see it in the list (SURV-01, SURV-06)
- Uploading a valid .xlsx file parses with exceljs and stores questions in questions-{id}.csv (SURV-02, SURV-04, DATA-01)
- Question types likert/open_ended/demographic are preserved through import (SURV-03)
- dimension field maps questions to GPTW sections (SURV-05)
- All new UI strings have entries in both en.json and mm.json
- writeRows() enables full-overwrite CSV writes needed by Plan 02 SMTP settings
- vitest passes, tsc passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-survey-creation-and-distribution/02-01-SUMMARY.md`
</output>
