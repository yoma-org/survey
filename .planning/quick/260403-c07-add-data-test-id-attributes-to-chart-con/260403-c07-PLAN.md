---
phase: quick
plan: 260403-c07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/ChartSection.tsx
  - src/components/dashboard/DashboardCharts.tsx
  - src/components/dashboard/MetricCard.tsx
  - src/components/dashboard/DashboardFilters.tsx
  - src/components/dashboard/SurveySelector.tsx
  - src/components/dashboard/ExportButtons.tsx
  - src/components/dashboard/PillarHeatmap.tsx
  - src/components/dashboard/EarlyWarningAlerts.tsx
  - src/components/dashboard/SentimentAnalysisCards.tsx
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "Every chart container in the dashboard has a unique data-test-id attribute"
    - "Key UI controls (filters, selectors, export buttons) have data-test-id attributes"
    - "Metric cards have data-test-id attributes identifying their purpose"
  artifacts:
    - path: "src/components/dashboard/ChartSection.tsx"
      provides: "data-test-id prop passthrough on chart container div"
    - path: "src/components/dashboard/DashboardCharts.tsx"
      provides: "data-test-id values passed to every ChartSection and standalone section"
  key_links:
    - from: "DashboardCharts.tsx"
      to: "ChartSection.tsx"
      via: "testId prop"
      pattern: "data-test-id"
---

<objective>
Add `data-test-id` attributes to all chart containers and important UI elements in the admin dashboard for automated testing and scraping support.

Purpose: Enable reliable element targeting for E2E tests, visual regression tools, and automated data extraction.
Output: All dashboard charts and key UI controls decorated with unique `data-test-id` attributes.
</objective>

<execution_context>
@/Users/tranhao/Desktop/Working/01_TTS/05_YOMA/survey/survey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tranhao/Desktop/Working/01_TTS/05_YOMA/survey/survey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/dashboard/ChartSection.tsx
@src/components/dashboard/DashboardCharts.tsx
@src/components/dashboard/MetricCard.tsx
@src/components/dashboard/DashboardFilters.tsx
@src/components/dashboard/SurveySelector.tsx
@src/components/dashboard/ExportButtons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testId prop to ChartSection and add data-test-id to MetricCard</name>
  <files>src/components/dashboard/ChartSection.tsx, src/components/dashboard/MetricCard.tsx</files>
  <action>
In ChartSection.tsx:
- Add optional `testId?: string` prop to ChartSectionProps interface
- Apply `data-test-id={testId}` to the outer `<div>` wrapper element
- If testId is undefined, omit the attribute (use spread: `{...(testId && { 'data-test-id': testId })}`)

In MetricCard.tsx:
- Add optional `testId?: string` prop to MetricCardProps interface
- Apply `data-test-id={testId}` to the outer `<div className="space-y-0.5">` element using the same conditional spread pattern
  </action>
  <verify>
    <automated>cd /Users/tranhao/Desktop/Working/01_TTS/05_YOMA/survey/survey && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ChartSection accepts and renders testId prop; MetricCard accepts and renders testId prop; no type errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire data-test-id values throughout DashboardCharts and dashboard controls</name>
  <files>src/components/dashboard/DashboardCharts.tsx, src/components/dashboard/DashboardFilters.tsx, src/components/dashboard/SurveySelector.tsx, src/components/dashboard/ExportButtons.tsx, src/components/dashboard/PillarHeatmap.tsx, src/components/dashboard/EarlyWarningAlerts.tsx, src/components/dashboard/SentimentAnalysisCards.tsx</files>
  <action>
In DashboardCharts.tsx, pass `testId` to every ChartSection instance and add `data-test-id` to standalone sections (ones that don't use ChartSection). Use these exact IDs:

**Hero MetricCards (pass testId prop):**
- Employee Engagement: `testId="metric-employee-engagement"`
- Great Place to Work: `testId="metric-great-place-to-work"`
- Response Rate: `testId="metric-response-rate"`
- Total Responses: `testId="metric-total-responses"`

**Overview tab ChartSections (pass testId prop):**
- EES Trend: `testId="chart-ees-trend"`
- Pillar Performance (DimensionBarChart): `testId="chart-pillar-performance"`
- Overall Sentiment (ResponseDonutChart): `testId="chart-overall-sentiment"`
- Industry Benchmark: `testId="chart-industry-benchmark"`
- Relationship Axes (RelationshipRadar): `testId="chart-relationship-axes"`
- Statement Rankings container (standalone div, not ChartSection): add `data-test-id="chart-statement-rankings"` to its outer ErrorBoundary sibling div (the one with the Tabs)
- Department Breakdown: `testId="chart-department-breakdown"`

**Deep Dive tab ChartSections:**
- Pillar Heatmap (standalone div): add `data-test-id="chart-pillar-heatmap"` to the outer div wrapping the heatmap
- Sub-Pillar Deep Dive: `testId="chart-sub-pillar-breakdown"`
- Relationship Statements: `testId="chart-relationship-statements"`
- Leadership Perspective: `testId="chart-leadership-comparison"`
- Confidence in Leadership: `testId="chart-leadership-confidence"`
- eNPS Detail: `testId="chart-enps-detail"`
- eNPS Legacy: `testId="chart-enps-legacy"`

**Workforce tab ChartSections:**
- Internal Benchmark: `testId="chart-internal-benchmark"`
- Tenure Journey: `testId="chart-tenure-journey"`
- Tenure Insights: `testId="chart-tenure-insights"`
- Early Warning (standalone div): add `data-test-id="chart-early-warning"` to its outer div

**Qualitative tab:**
- Sentiment Analysis (standalone div): add `data-test-id="chart-sentiment-analysis"` to its outer div

In DashboardFilters.tsx:
- Add `data-test-id="filter-organization"` to the organization Select's SelectTrigger
- Add `data-test-id="filter-department"` to the department Select's SelectTrigger
- Add `data-test-id="dashboard-filters"` to the outer wrapper div

In SurveySelector.tsx:
- Add `data-test-id="survey-selector"` to the SelectTrigger element

In ExportButtons.tsx:
- Add `data-test-id="export-dashboard-csv"` to the dashboard export button
- Add `data-test-id="export-rankings-csv"` to the rankings export button
- Add `data-test-id="export-buttons"` to the outer wrapper div
  </action>
  <verify>
    <automated>cd /Users/tranhao/Desktop/Working/01_TTS/05_YOMA/survey/survey && npx tsc --noEmit --pretty 2>&1 | head -30 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <done>All chart containers have unique data-test-id attributes. All filter controls, survey selector, and export buttons have data-test-id attributes. TypeScript compiles cleanly. Lint passes.</done>
</task>

</tasks>

<verification>
- `grep -r "data-test-id" src/components/dashboard/ | wc -l` returns 25+ matches
- `npx tsc --noEmit` passes with no errors
- `pnpm lint` passes
- Every ChartSection in DashboardCharts.tsx has a testId prop
- Every standalone chart div has a data-test-id attribute
</verification>

<success_criteria>
- Every chart container in the dashboard has a unique, descriptive `data-test-id`
- Key UI controls (org filter, dept filter, survey selector, export buttons) have `data-test-id` attributes
- All four metric cards have `data-test-id` attributes
- No TypeScript or lint errors introduced
</success_criteria>

<output>
After completion, create `.planning/quick/260403-c07-add-data-test-id-attributes-to-chart-con/260403-c07-SUMMARY.md`
</output>
